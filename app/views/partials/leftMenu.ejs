<div class="col-lg-3">

    <h1 class="my-4">O'Fig</h1>
    
    <h5>Catégories</h5>
    <div class="list-group">
      <% if (locals.count && locals.count.length > 0) { %>
       
      
      <% for(const counted of count ) { %>
      
      <a class="list-group-item d-flex justify-content-between align-items-center" href="/category/<%= counted.category %>">
          <%= counted.category %>
        <span class="badge badge-primary badge-pill"><%= counted.count %></span>
      </a>

      <% } %>
      <% } %>
    </div>

  </div>
  <!-- /.col-lg-3 -->